<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hifunki.funki">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--读取文件权限-->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!--写入数据权限-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/iv_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.login.MainActivity" />

        <!-- 游客必填信息页 -->
        <activity
            android:name=".ui.activity.login.VisitorFillActivity"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--android:launchMode="singleTop"-->
        <!-- 登录界面 -->
        <activity
            android:name=".ui.activity.login.LoginActivity"
            android:launchMode="singleTask"
            android:screenOrientation="portrait" />
        <!-- 国家列表页 -->
        <activity
            android:name=".ui.activity.login.CountyListActivity"
            android:screenOrientation="portrait" />
        <!-- 注册界面 -->
        <activity
            android:name=".ui.activity.login.RegisterActivity"
            android:screenOrientation="portrait" />
        <!-- 图形验证码界面 -->
        <activity
            android:name=".ui.activity.login.PwdGraphActivity"
            android:screenOrientation="portrait" />
        <!-- 邮箱验证码界面 -->
        <activity
            android:name=".ui.activity.login.EmailGraphActivity"
            android:screenOrientation="portrait" />
        <!-- 找回密码界面 -->
        <activity android:name=".ui.activity.login.FindPwdActivity" />
        <!-- 注册认证界面 -->
        <activity
            android:name=".ui.activity.login.RegisterAuthActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.activity.login.SetPwdActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.activity.login.PwdSuccessActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.activity.login.CountryResultActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".ui.activity.imageselect.SelectImageActivity"></activity>

        <activity
            android:name="com.hifunki.funki.ui.activity.imageselect.GalleryPickActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:screenOrientation="portrait"
            android:theme="@style/GalleryTheme" />

        <activity
            android:name="com.hifunki.funki.ui.activity.imageselect.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />


        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.hifunki.funki.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>

    </application>


</manifest>